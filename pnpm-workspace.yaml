# Workspace pnpm do robo-felipe.
# Apenas código TypeScript vive no workspace; android/, firmware/ e esp32-server/
# têm toolchains próprias (Gradle, ESP-IDF, Docker) e ficam de fora.
packages:
  - core
  - packages/*
allowBuilds:
  better-sqlite3: true
  esbuild: true
